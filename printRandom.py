import random
for i in range(5):
    print(random.randint(1,10))

# This code generates and prints 5 random integers between 1 and 10.
# It uses the random module to generate the integers.
# Each time you run this code, it will produce different random numbers.
# You can change the range by modifying the arguments of randint.